# Simple install MYSQL using docker-compose

### Clone repo
```
mkdir mysql
cd mysql
git clone https://github.com/galihamarbarkah/MYSQL-8-docker-compose.git .
```

### run script
```
docker-compose up -d
```
